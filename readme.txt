create a new file with index.js and npm inti answer all questions.
install all required dependencies like express, dotenv, mongoose, morgan and helmet packages.
Now create the new model and starts with the capital letter write required schema and exports with the module.exports with new mongoose model.
Now create the new routes folder in this create a new file with small letter and import the mongoose model.
In the routes file import the router from express and create new post or get route.
In the index.js file import route file and use with a app.use("endpoints", routeFileName).
You must the aysnc function in the routes file.
Create a new route for register and login install bcrypt or crypt-js for hashing the password. 